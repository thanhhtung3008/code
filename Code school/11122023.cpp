#include <iostream>
using namespace std;

string decToHex(string dec) {
    long long len=dec.size()-1;
    while(i<len)
    {
        if(dec[i])

        i++;
    }
}

int main() {
    string dec = "247";
    cout << decToHex(dec) << endl;
    return 0;
}


#include <iostream>
using namespace std;
int main()
{
    /*
    // Bai 1
    float a,b;
    cin >> a >> b;
    cout << 2*(a+b);

    // Bai 2
    float d1,d2;
    cin >> d1 >> d2;
    cout << (d1*d2/2);

    // Bai 3
    float d,r;
    cin >> d >> r;
    cout << ((d+r)*2) << endl;
    cout << (d*r);
*/
/*
    // Bai 4
    int m,n,s=0;
    cin >> m >> n;
    for (int i=m;i<=n;i+=2)
        s++;
    cout << s;
    /*
    // Bai 5
    int n,s=0;
    cin >> n;
    for(int i=1;i<=n;i++)
        s+=i;
    cout << s;

    // Bai 6
    int m,n,s=0;
    cin >> m >> n;
    for (int i=m;i<=n;i+=2)
        s+=i;
    cout << s;

    // Bai 7
    int x,y;
    cin >> x >> y;
    if ((1<=x && x<=8)&&(1<=y&&y<=8))
        if (x==y||(x+y)%2==0)
            cout << "Trang";
        else
            cout << "Den";
    else
        cout << "Loi";
*/
/*
    // Bai 8
    int N,t,c,dv,s;
    cin >> N;
    if (N>0&& N<1000)
    {
        t=N/100;
        c=(N%100)/10;
        dv=N%10;
    }
    s = t+c+dv;
    cout << s;
    */

    //
}

